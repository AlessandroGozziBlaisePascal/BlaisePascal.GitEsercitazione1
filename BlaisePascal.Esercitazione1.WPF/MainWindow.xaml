<Window x:Class="BlaisePascal.Esercitazione1.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlaisePascal.Esercitazione1.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="DATI AUTORE" FontWeight="Bold" Margin="0,0,0,10" Foreground="#2C3E50"/>

            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="Labels"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Nome:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtNome" Grid.Row="0" Grid.Column="1" Margin="5,2" Height="25"/>

                <Label Content="Cognome:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtCognome" Grid.Row="1" Grid.Column="1" Margin="5,2" Height="25"/>

                <Label Content="Nome d'arte:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtNomeArte" Grid.Row="2" Grid.Column="1" Margin="5,2" Height="25"/>
            </Grid>

            <Separator Margin="0,5,0,15"/>

            <TextBlock Text="DATI BRANO" FontWeight="Bold" Margin="0,0,0,10" Foreground="#2C3E50"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Titolo:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" MinWidth="80"/>
                <TextBox x:Name="txtTitoloBrano" Grid.Row="0" Grid.Column="1" Margin="5,2" Height="25"/>

                <Label Content="Durata (min):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox x:Name="txtDurata" Grid.Row="1" Grid.Column="1" Margin="5,2" Height="25"/>
            </Grid>

            <Button Content="Aggiungi Brano al CD" Click="BtnAggiungi_Click" Margin="0,20,0,0" Height="35" Background="LightBlue" FontWeight="Bold"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="DETTAGLI CD" FontWeight="Bold" Margin="0,0,0,5"/>
            <DockPanel Margin="0,0,0,10">
                <Label Content="Titolo CD:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtTitoloCD" FontWeight="Bold" Height="25" VerticalContentAlignment="Center"/>
            </DockPanel>

            <ListBox x:Name="lstBrani" Height="220" Margin="0,5" BorderBrush="#BDC3C7"/>

            <TextBlock x:Name="lblStatus" Text="CD Vuoto" Foreground="Gray" FontStyle="Italic" Margin="0,5"/>
            <Button Content="Test Funzionalità CD" Click="BtnTest_Click" Height="30" Margin="0,5" Background="#ECF0F1"/>
        </StackPanel>
    </Grid>
</Window>
